var number = 7;

for (let x = 2; x < number; x++) {
    if (number % x == 0) {
        console.log(`${number} is a prime number`);
        break;
    } else {
        console.log(`${number} is a prime number`);
        break;
    }
}